<h1>Enter Improvements you want to make to your house</h1>

<%= form_for @permit do |f| %>
  <% if @permit.errors.any? %>
    <div class="error_messages">
      <h2><%= pluralize(@permit.errors.count, "error") %> prohibited this permit from being saved:</h2>
      <ul>
      <% @permit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Addition" %> <br />
    <%= f.text_field :addition%> <br />
  </div>
  <div class="actions">
    <%= f.submit "Next" %>
  </div>  
<% end %>